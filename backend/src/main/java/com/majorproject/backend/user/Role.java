package com.majorproject.backend.user;

/**
 * User roles for the platform.
 * STUDENT is the default role assigned on registration.
 * Can be extended with ADMIN, MENTOR, etc. in future phases.
 */
public enum Role {
    STUDENT
}
